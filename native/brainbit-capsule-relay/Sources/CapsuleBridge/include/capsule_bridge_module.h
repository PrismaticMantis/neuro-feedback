/**
 * Minimal umbrella for Swift `import CapsuleBridge` — only APIs needed until the full
 * `capsule_bridge.h` graph is aligned with SDK (e.g. CPPG type order).
 */
#pragma once

#include "Capsule/CClient.h"
#include "Capsule/CDeviceLocator.h"
#include "Capsule/CDevice.h"
